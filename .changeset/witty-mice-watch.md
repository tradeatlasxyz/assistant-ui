---
"@assistant-ui/react": patch
"@assistant-ui/ui": patch
---

fix(thread): add opt-in per-thread viewport scroll restoration on thread switch and clear stale saved positions when jumping back to latest.
